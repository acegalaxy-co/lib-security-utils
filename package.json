{
  "name": "@acegalaxy/lib-security-utils",
  "version": "0.3.0",
  "private": true,
  "description": "Shared security primitives (audit logging, caller validation, sliding-window rate limiting, replay guard) consumed cross-project via git-dependency.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./audit-log": "./dist/audit-log/index.js",
    "./caller-validator": "./dist/caller-validator/index.js",
    "./rate-limit": "./dist/rate-limit/index.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "node --import tsx --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^20",
    "tsx": "^4"
  }
}
